{
  "scripts": {
    "dev": "npx eleventy --serve --output=docs",
    "deploy": "npx eleventy && gh-pages -d _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "gh-pages": "^6.3.0"
  }
}
